style.
    img{
        max-width:100px;
        filter: drop-shadow(7px 13px 5px #00000042);
    }
    img.cloudy{
        max-width:100px;
        left: -10px;
        filter: drop-shadow(7px 13px 5px #00000042);
        position: relative;
        animation: cloud;
        animation-duration: 4s;
        animation-iteration-count: infinite;
        animation-timing-function: linear
    }
    @keyframes cloud {
        0% {left: -10px;}
        25% {left: 10px;}
        50% {left: -10px;}
        100%{left: 10px;}
    }
meta(name='viewport', content='width=device-width, initial-scale=1.0')
link(rel='manifest', href='/js/manifest.json')
link(rel='shortcut icon', href='/favicon.png')
meta(name='theme-color' content='#37afa3')
body(style="background:#37afa3;margin:0;padding:0")
    //- If searched show. Otherwise hide this div
    if city
        div(style="text-align:center;color:white;margin:0 auto;padding:30px")
            h1 #{city}
            if status=="Clouds"
                img(class="cloudy", src="/images/icons/cloudy.png", alt="")
            if status=="Drizzle"
                img(class="cloudy",src="/images/icons/rain.png", alt="")
            if status=="Thunderstorm"
                img(class="cloudy",src="/images/icons/storm.png", alt="")
            if status=="Rain"
                img(class="cloudy",src="/images/icons/rain.png", alt="")
            if status=="Snow"
                img(class="cloudy",src="/images/icons/snow.png", alt="")
            if status=="Clear"
                img(src="/images/icons/sunny.png", alt="")
            if status=="Mist"
                img(class="cloudy",src="/images/icons/fog.png", alt="")
            h3 #{status} and the temprature is #{temp}°C
    else
        div(style="color:white;margin:100px auto;text-align:center;padding: 14px;")
            h2 See the Forecast of Your Desired City!
    div(style="margin:0 auto;text-align:center;padding: 14px;")
        form(method='GET',action="/weather")
            input(type="text", name='q', placeholder='City',style="border: none; padding: 10px 90px 10px 10px;")
            button(type='submit',style="border: none; background: #2c7f9e; color: white; font-size: 16px; padding: 10px 83px; border-radius: 6px; display: block; margin: 10px auto;") Look Up